# AI Deal Creator — Python dependencies
# Phase 1 MVP: only stdlib + pydantic required to run the CLI demo.

# Validation
pydantic>=2.0

# Testing
pytest>=7.0

# ---- API server ----
# Install to run `uvicorn app.api.main:app --reload`
fastapi>=0.111
uvicorn>=0.29
httpx>=0.27

# ---- Phase 2+ dependencies (uncomment as integrations are built) ----
# MCP SDK
# mcp>=1.0

# HTTP client (for MCP server calls)
# httpx>=0.27

# Claude SDK (for subagent orchestration)
# anthropic>=0.40
